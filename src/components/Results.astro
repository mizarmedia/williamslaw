---
const results = [
  {
    amount: 'Mid-Six-Figure',
    type: 'Settlement',
    description: 'Southwest Airlines employment case resulting in substantial compensation for our client.',
    category: 'Employment Law'
  },
  {
    amount: 'Favorable',
    type: 'Verdict',
    description: 'Civil rights victory against Middleton Police Department. Justice served.',
    category: 'Civil Rights'
  },
  {
    amount: 'Jury',
    type: 'Verdict',
    description: 'Wrongful termination case won before a jury. Full vindication for our client.',
    category: 'Employment Law'
  }
];
---

<section id="results" class="py-20 md:py-28 bg-offwhite">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-16 fade-in">
      <h2 class="font-[family-name:var(--font-display)] text-3xl md:text-4xl lg:text-5xl font-bold text-navy-900 mb-4">
        Case Results
      </h2>
      <p class="text-lg text-text-gray max-w-2xl mx-auto">
        A track record of successful outcomes for Idaho clients.
      </p>
    </div>

    <!-- Results Cards -->
    <div class="grid md:grid-cols-3 gap-8">
      {results.map((result, index) => (
        <div
          class="bg-white p-8 rounded-lg shadow-sm border border-gray-100 text-center fade-in"
          style={`transition-delay: ${index * 100}ms`}
        >
          <span class="inline-block px-3 py-1 bg-gold-500/10 text-gold-500 text-xs font-semibold uppercase tracking-wide rounded-full mb-4">
            {result.category}
          </span>
          <div class="font-[family-name:var(--font-display)] text-3xl md:text-4xl font-bold text-navy-900 mb-1">
            {result.amount}
          </div>
          <div class="text-gold-500 font-semibold uppercase tracking-wide text-sm mb-4">
            {result.type}
          </div>
          <p class="text-text-gray text-sm leading-relaxed">
            {result.description}
          </p>
        </div>
      ))}
    </div>

    <!-- Disclaimer -->
    <p class="text-center text-xs text-text-gray/60 mt-8 max-w-2xl mx-auto">
      Every case is different. Past results do not guarantee similar outcomes. The specific facts and circumstances of your case will determine the results.
    </p>
  </div>
</section>
